# 简谱转换工具

**作者：Tsukamotoshio**

本工具用于将五线谱 PDF 批量转换为简谱 PDF，并可按需同时生成 MIDI 文件。

---

## 功能简介

- 批量读取 `Input` 文件夹中的五线谱乐谱文件
- 自动识别并转换为简谱 PDF，转换完毕后自动打开 `Output` 文件夹
- 可选同时生成 MIDI 文件
- 已转换过的文件自动跳过，避免重复转换
- 支持中文、日文等文件名场景的兼容处理

### 支持的输入格式

| 格式 | 说明 |
|------|------|
| `.pdf` | 五线谱 PDF（推荐，支持多页）|
| `.png` | 五线谱图片 |
| `.jpg` / `.jpeg` | 五线谱图片 |

---

## 使用方法

1. 将待转换的五线谱文件（PDF / PNG / JPG）放入 `Input` 文件夹
2. 运行程序 `ConvertTool.exe`
3. 按提示确认是否开始转换、是否生成 MIDI
4. 转换结果会保存到 `Output` 文件夹

---

## 目录说明

- `Input/`：放入原始五线谱文件（PDF / PNG / JPG）
- `Output/`：保存生成的简谱 PDF / MIDI
- `logs/`：自动记录运行日志
- `THIRD_PARTY_NOTICES.md`：第三方组件许可证说明

---

## 已知缺陷与局限

- **识别准确率受乐谱质量影响**：Audiveris 基于 OCR 和机器学习识别，扫描模糊或排版复杂的乐谱可能出现错音、漏音
- **多声部支持有限**：多声部、和弦较多的乐谱转换结果可能只保留主旋律，部分音符会丢失
- **不支持歌词输出**：目前转换结果中不含歌词，仅输出音符
- **处理速度较慢**：Audiveris 启动和识别耗时较长，多页 PDF 可能需要数分钟
- **调号 / 拍号边缘情况**：少数非常规调号或变拍子乐谱的转换结果可能不准确

---

## 署名说明

本项目的**整合、脚本编写、功能调整与打包工作**由 **Tsukamotoshio** 完成。

如果对外分发，建议保留本署名与 `THIRD_PARTY_NOTICES.md`，以区分：
- **整合、开发、打包工作**：Tsukamotoshio
- **第三方组件版权与许可证**：仍归其各自原作者所有

---

## 许可证

本项目采用 **MIT License** 授权，详见 `LICENSE` 文件。

本工具包含第三方组件（如 Audiveris、LilyPond、music21 等），
其各自的版权与许可证信息请参见 `THIRD_PARTY_NOTICES.md`。

---

## 开发说明

本项目采用 **Vibe Coding** 方式开发，由 **GitHub Copilot** 辅助完成代码编写与调试。

---

## 更新日志

### v0.1.1
- 转换完成后自动打开 `Output` 文件夹
- 转换全部失败或无实际新增文件（如全部为重复文件）时，提示"已完成，但无新增文件"而非成功提示
- 新增支持 PNG / JPG / JPEG 格式输入（除 PDF 外）
- 修复：输出简谱 PDF 末尾出现 "Music engraving by LilyPond" 水印文字的问题
- 完善已知缺陷与局限说明

---

## 版本

当前版本：`0.1.1`
