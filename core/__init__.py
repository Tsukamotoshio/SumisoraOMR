# core package
# Sub-packages:
#   image/  — image_preprocess, staff_slicer, quality_score
#   omr/    — audiveris_runner, homr_runner, engine_router, omr_validator, dl_fix, worker_main
#   music/  — jianpu_core, jianpu_txt_editor, tie_reconstruction, transposer
#   render/ — lilypond_runner, renderer
#   app/    — pipeline, tui, backend, runtime_finder
# Root modules: config, utils
