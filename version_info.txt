# UTF-8
#
# PyInstaller VERSIONINFO resource file for the SumisoraOMR bootloader exe.
# Wired into SumisoraOMR.spec via `version='version_info.txt'` on the EXE() call.
#
# This controls what Windows shows in:
#   - File Explorer's Properties → Details tab on the bootloader exe
#   - Right-click context menu's "App name" entry (when the bootloader is the
#     visible process; in our case the Flutter window's exe takes over the
#     taskbar entry, but we want the bootloader to be branded too)
#
# Bump the numbers and the version strings together when releasing a new version.

VSVersionInfo(
  ffi=FixedFileInfo(
    filevers=(0, 3, 3, 0),
    prodvers=(0, 3, 3, 0),
    mask=0x3F,
    flags=0x0,
    OS=0x40004,
    fileType=0x1,
    subtype=0x0,
    date=(0, 0),
  ),
  kids=[
    StringFileInfo(
      [
      StringTable(
        u'040904B0',
        [StringStruct(u'CompanyName',      u'Tsukamotoshio'),
        StringStruct(u'FileDescription',  u'SumisoraOMR'),
        StringStruct(u'FileVersion',      u'0.3.3.0'),
        StringStruct(u'InternalName',     u'SumisoraOMR'),
        StringStruct(u'LegalCopyright',   u'Copyright (C) 2026 Tsukamotoshio'),
        StringStruct(u'OriginalFilename', u'SumisoraOMR.exe'),
        StringStruct(u'ProductName',      u'SumisoraOMR'),
        StringStruct(u'ProductVersion',   u'0.3.3.0')])
      ]),
    VarFileInfo([VarStruct(u'Translation', [0x0409, 1200])])
  ]
)
